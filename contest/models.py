from django.db import models

# Create your models here.
# Author (first, last name, birth date), Song (name, year, album, yt link, sortOrder), Album (author, name, cover*), SongVotes (song, vote_hash), ContestSubmission (first, last name, telephone, email, context)
